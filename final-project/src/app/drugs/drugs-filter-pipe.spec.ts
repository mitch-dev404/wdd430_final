import { DrugsFilterPipe } from './drugs-filter-pipe';

describe('DrugsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DrugsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
